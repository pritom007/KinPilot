{
  "name": "kinpilot",
  "private": true,
  "workspaces": ["web", "functions"],
  "scripts": {
    "build": "npm run build -w functions && npm run build -w web",
    "test": "npm run test -w functions && npm run test -w web",
    "typecheck": "npm run typecheck -w functions && npm run typecheck -w web"
  },
  "engines": { "node": ">=20" }
}
